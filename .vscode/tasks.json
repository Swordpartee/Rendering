{
    "tasks": [
        {
            "type": "shell",
            "label": "Build with Makefile",
            "command": "mingw32-make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build using Makefile (automatically finds all .cpp files)"
        },
        {
            "type": "shell",
            "label": "Run with Makefile",
            "command": "mingw32-make",
            "args": ["run"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "detail": "Build and run using Makefile"
        }
    ],
    "version": "2.0.0"
}